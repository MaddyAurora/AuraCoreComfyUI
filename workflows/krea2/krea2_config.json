{
  "_comment": "Sidecar config for krea2.json\nThis file tells app.py exactly which node IDs to inject UI values into.\nThe workflow JSON must sit in the same folder with the same base name.",

  "prompt_node":     { "id": "37", "field": "value" },
  "negative_prompt": false,

  "seed_node":       { "id": "26", "field": "seed" },
  "steps_node":      { "id": "26", "field": "steps" },
  "cfg_node":        { "id": "26", "field": "cfg" },

  "resolution_node": {
    "id": "20",
    "mode": "aspect_ratio",
    "aspect_ratio_field": "aspect_ratio",
    "megapixels_field": "megapixels",
    "default_aspect_ratio": "1:1 (Square)",
    "default_megapixels": 1.5,
    "multiple": 8
  }
}
